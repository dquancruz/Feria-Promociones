// Shared types and business logic used by both apps/frontend and apps/backend
// (e.g. the catalog item shape and the discount calculation rules) will live
// here so both sides stay in sync. Intentionally empty until that logic is
// implemented — see docs/discount-logic.md for the spec.

export {};
